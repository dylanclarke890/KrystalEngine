Source: https://dom.spec.whatwg.org

1.3. Selectors - https://dom.spec.whatwg.org/#selectors
 - scope-match a selectors string - requires CSS selector parsing and matching, which is not yet implemented.
     - https://dom.spec.whatwg.org/#scope-match-a-selectors-string

2.3. Legacy extensions to the Window interface - https://dom.spec.whatwg.org/#interface-window-extensions
 - Window interface - readonly attribute (Event or undefined) event; not implemented, as Window interface is not supported.
     - https://dom.spec.whatwg.org/#dom-window-event

2.5. Constructing events - https://dom.spec.whatwg.org/#constructing-events
 - check this has been implemented fully

2.7. Interface EventTarget - https://dom.spec.whatwg.org/#eventtarget
 - AddEventListener - service workers currently not supported.
     - https://dom.spec.whatwg.org/#add-an-event-listener
 - RemoveEventListener - service workers currently not supported.
     - https://dom.spec.whatwg.org/#remove-an-event-listener
 - Add/RemoveEventListener - We need a way of passing a lambda instead of a EventListenerCallback as it's awkward to use.
   The issue to solve is how to compare the lambda to the callback when adding/removing listeners. Take a similar approach to AbortAlgorithmCallback

2.8. Observing event listeners - https://dom.spec.whatwg.org/#observing-event-listeners
 - To legacy-obtain service worker fetch event listener callbacks algorithm not implemented, as service workers are not supported.
     - https://dom.spec.whatwg.org/#legacy-obtain-service-worker-fetch-event-listener-callbacks

2.9. Dispatching events - https://dom.spec.whatwg.org/#dispatching-events
 - DispatchToTarget - Let targetOverride be target, if legacy target override flag is not given, and target’s associated Document otherwise. not implemented, as Window interface is not supported.
     - https://dom.spec.whatwg.org/#concept-event-dispatch
 - InnerInvoke - Let global be listener callback’s associated realm’s global object not implemented, as it uses the legacy extension to the Window interface, which is not supported.
 - InnerInvoke - If global is a Window object, then record timing info for event listener given event and listener. not implemented, as Window interface is not supported.
 - InnerInvoke - Call a user object’s operation with listener’s callback, "handleEvent", « event », and event’s currentTarget attribute value. Not supported as this is a JavaScript concept.
 - InnerInvoke - Should have some way of logging exceptions potentially thrown by the user’s operation.
 - InnerInvoke - If global is a Window object, then set global’s current event to currentEvent. Same as above, not implemented, as Window interface is not supported.
     - https://dom.spec.whatwg.org/#concept-event-listener-inner-invoke

3.2. Interface AbortSignal - https://dom.spec.whatwg.org/#abortsignal
 - onabort EventHandler attribute; Haven't figured out how to support EventHandler attributes yet, as this is a JavaScript concept.
     - https://dom.spec.whatwg.org/#dom-abortsignal-onabort
 - Timeout - Task queues and timers are required to implement this.
     - https://dom.spec.whatwg.org/#concept-abortsignal-timeout
 - Let resultSignal be a new object implementing signalInterface using realm. We only have one AbortSignal implementation, so this is not implemented.
     - https://dom.spec.whatwg.org/#create-a-dependent-abort-signal

3.2.1. Garbage collection - https://dom.spec.whatwg.org/#garbage-collection
 - A non-aborted dependent AbortSignal object must not be garbage collected while its source signals is non-empty and it has registered event listeners for its abort event
   or its abort algorithms is non-empty - I don't understand this properly enough to know how to implement it, so this is not implemented.
     - - https://dom.spec.whatwg.org/#garbage-collection

4.2.2.5. Signaling slot change - https://dom.spec.whatwg.org/#signaling-slot-change
 - Signaling slot change - Append slot to slot’s relevant agent’s signal slots. Don't have 'relevant agents' or 'signal slots'
 - Signaling slot change - Queue mutation observer microtask. Don't have microtasks or task queues, so this is not implemented.
     - https://dom.spec.whatwg.org/#signaling-slot-change

4.2.3. Mutation algorithms - https://dom.spec.whatwg.org/#mutation-algorithms
 - Insert - Custom element reactions and upgrade steps are required.
     - https://dom.spec.whatwg.org/#concept-node-insert
 - Move - Custom element reactions are required.
     - https://dom.spec.whatwg.org/#concept-node-move
 - Remove - Custom element reactions are required.
 - Remove - Transient mutation observers are required at the end of this algorithm.
     - https://dom.spec.whatwg.org/#concept-node-remove

4.2.6. Mixin ParentNode - https://dom.spec.whatwg.org/#interface-parentnode
 - QuerySelector/QuerySelectorAll - Requires selector parsing and matching.
     - https://dom.spec.whatwg.org/#dom-parentnode-queryselector
     
4.2.10.1. Interface NodeList - https://dom.spec.whatwg.org/#interface-nodelist
 - LiveNodeList - This is implemented but inefficient, it iterates through the entire tree to find matching nodes every time.
 
4.2.10.2. Interface HTMLCollection - https://dom.spec.whatwg.org/#interface-htmlcollection
 - LiveHTMLCollection - This is implemented but inefficient, it iterates through the entire tree to find matching nodes every time.

4.3. Mutation observers - https://dom.spec.whatwg.org/#mutation-observers
 - Node includes RegisteredObserver/TransientRegisteredObserver/MutationRecord, is there a way to forward declare these to avoid them being included in Node.hpp?
 - Each similar-origin window agent has a mutation observer microtask queued (a boolean), which is initially false.
   Not sure how to implement window agent as it's a JavaScript concept.
      - https://dom.spec.whatwg.org/#mutation-observer-compound-microtask-queued-flag
 - Each similar-origin window agent also has pending mutation observers (a set of zero or more MutationObserver objects), which is initially empty.
   Not sure how to implement window agent as it's a JavaScript concept.
      - https://dom.spec.whatwg.org/#mutation-observer-list
 - Queuing a mutation record - Requires microtasks and task queues, which are not implemented yet.
     - https://dom.spec.whatwg.org/#queue-a-mutation-observer-compound-microtask
 - Notify mutation observers - Requires microtasks and task queues, which are not implemented yet.
     - https://dom.spec.whatwg.org/#notify-mutation-observers

4.3.2. Queuing a mutation record - https://dom.spec.whatwg.org/#queueing-a-mutation-record
 - Queue a mutation record - Append observer to the surrounding agent’s pending mutation observers. Don't have 'surrounding agents' or 'pending mutation observers'.
   - https://dom.spec.whatwg.org/#queue-a-mutation-record

4.4 Interface Node - https://dom.spec.whatwg.org/#interface-node
 - The baseURI getter steps are to return this’s node document’s document base URL, serialized. URL spec. Unlikely to support this.
     - https://dom.spec.whatwg.org/#dom-node-baseuri
 - CloneSingleNode - Encoding is not currently passed to the cloned Document. Needs to
 - CloneSingleNode - Domain is not currently passed to the cloned Document. Unlikely to support this.
 - CloneSingleNode - URL is not currently passed to the cloned Document. URL spec. Unlikely to support this.
     - https://dom.spec.whatwg.org/#clone-a-single-node
 - GetElementsByTagName/GetElementsByTagNameNS/GetElementsByClassName - implemented but inefficient, it iterates through the entire tree to find matching nodes every time.
     - https://dom.spec.whatwg.org/#dom-node-getelementsbytagname
     - https://dom.spec.whatwg.org/#dom-node-getelementsbytagnamens
     - https://dom.spec.whatwg.org/#dom-node-getelementsbyclassname
 - LastChild/FirstChild/ParentNode/PreviousSibling/NextSibling - These currently return RawPtr but they should return RefPtr instead for the public API.
     - https://dom.spec.whatwg.org/#dom-node-lastchild

4.5 Interface Document
 - DocumentURI - URL spec. Unlikely to support this.
     - https://dom.spec.whatwg.org/#dom-document-documenturi

TODO:
CSS-SELECTORS
  - Separate spec implementation required first, significant work to implement CSS selector parsing and matching.
  - 1.3 Selectors - https://dom.spec.whatwg.org/#selectors
  - 4.9 Interface Element (Closest/Matches) - https://dom.spec.whatwg.org/#dom-element-closest
  - 4.2.6 Mixin ParentNode (QuerySelector/QuerySelectorAll) - https://dom.spec.whatwg.org/#dom-parentnode-queryselector

URLS
  - Separate spec implementation required first, fair bit of work to parse and serialize URLs.
  - 4.4 Interface Node (BaseURI) - https://dom.spec.whatwg.org/#dom-node-baseuri
  - 4.5 Interface Document (DocumentURI) - https://dom.spec.whatwg.org/#dom-document-documenturi

CUSTOM-ELEMENTS
  - Separate spec implementation required first, significant work required.
  - 4.2.3 Mutation algorithms (Insert/Remove) - https://dom.spec.whatwg.org/#mutation-algorithms
    - Custom element reactions and upgrade steps are required.

EVENT-LOOP
  - 4.3.2 Queuing a mutation record - https://dom.spec.whatwg.org/#mutation-observers
  - 3.1 Interface AbortController - https://dom.spec.whatwg.org/#interface-abortcontroller
  - 4.2.2.5 Signaling slot change - https://dom.spec.whatwg.org/#signaling-slot-change
  
SLOTTABLES
  - 4.2.2.1 Slots and 4.2.2.2 Slottables
    - Slots/Slottable attribute changes (name/slot attribute) missing test cases.
  - 4.2.2.3 Finding slots and slottables and 4.2.2.4 Assigning slottables and slots
    - Missing test cases.

MUTATION-OBSERVERS
  - 4.3.1 Interface MutationObserver - https://dom.spec.whatwg.org/#interface-mutationobserver
    - Missing test cases for observing attribute changes, character data changes, child list changes, subtree changes, disconnecting observers, and taking records.

Section 4.8 Interface ShadowRoot - https://dom.spec.whatwg.org/#interface-shadowroot
  - 'onslotchange' event handler and 'slotchange' event support.

Section 4.9 Interface Element - https://dom.spec.whatwg.org/#interface-element
  - AttachShadow and ShadowRoot missing test cases.

4.5.1 Interface DOMImplementation

4.6 Interface DocumentType

4.7 Interface DocumentFragment

4.9.1 Interface NamedNodeMap

4.9.2 Interface Attr

4.10 Interface CharacterData

4.11 Interface Text

4.12 Interface CDATASection

4.13 Interface ProcessingInstruction

4.14 Interface Comment

======================================================================
5. RANGES
======================================================================

5.2 Boundary points

5.3 Interface AbstractRange

5.4 Interface StaticRange

5.5 Interface Range

======================================================================
6. TRAVERSAL
======================================================================

6.0 Traversal

6.1 Interface NodeIterator

6.2 Interface TreeWalker

6.3 Interface NodeFilter

======================================================================
7. SETS
======================================================================

7.1 Interface DOMTokenList