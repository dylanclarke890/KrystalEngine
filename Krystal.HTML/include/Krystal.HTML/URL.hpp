#pragma once

#include "Krystal.HTML/DOMString.hpp"

namespace Krys::HTML
{
  // TODO(impl): URL - This is it's own spec, need to decide whether it's worth the effort.
  /// @see https://url.spec.whatwg.org/#url-representation
  struct URL
  {
  };
}