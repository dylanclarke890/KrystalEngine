#name
Emits less than sign, solidus, and characters when tag name doesn't match
#data
span©
#initial-state
RCDATAEndTagName
#expected-state
RCDATA
#tokens
Character(</span©)

#name
Emits less than sign, solidus, and characters when EOF reached
#data
di
#initial-state
RCDATAEndTagName
#append-eof
#tokens
Character(</di)
EndOfFile

#name
Emits less than sign, solidus, and characters when invalid tag name character encountered
#data
d!v>
#initial-state
RCDATAEndTagName
#expected-state
RCDATA
#tokens
Character(</d!v>)

#name
switches to BeforeAttributeName when parsing whitespace and end tag name matches start tag
#data
<div></div 
#expected-state
BeforeAttributeName
#tokens
StartTag(div, SelfClosing=false)

#name
switches to SelfClosingStartTag when parsing solidus and end tag name matches start tag
#data
<div></div/
#expected-state
SelfClosingStartTag
#tokens
StartTag(div, SelfClosing=false)

#name
emits end tag token when parsing GreaterThanSign end tag name matches start tag
#data
<div></div>
#tokens
StartTag(div, SelfClosing=false)
EndTag(div)
