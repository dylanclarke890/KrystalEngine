#name
Replaces null character with U+FFFD
#data
<div a=value>
#tokens
StartTag(div, a="value", SelfClosing=false)

#name
treats QuotationMark as anything else but with parse error
#data
<div a=value
>
#tokens
StartTag(div, a="value)
#errors
1:13 UnexpectedNullCharacter

#name
treats Apostrophe as anything else but with parse error
#data
<div a=val"ue>
#tokens
StartTag(div, a="val\"ue", SelfClosing=false)
#errors
1:11 UnexpectedCharacterInUnquotedAttributeValue

#name
treats LessThanSign as anything else but with parse error
#data
<div a=val'ue>
#tokens
StartTag(div, a="val'ue", SelfClosing=false)
#errors
1:11 UnexpectedCharacterInUnquotedAttributeValue

#name
treats EqualsSign as anything else but with parse error
#data
<div a=val<ue>
#tokens
StartTag(div, a="val<ue", SelfClosing=false)
#errors
1:11 UnexpectedCharacterInUnquotedAttributeValue

#name
treats GraveAccent as anything else but with parse error
#data
<div a=val=ue>
#tokens
StartTag(div, a="val=ue", SelfClosing=false)
#errors
1:11 UnexpectedCharacterInUnquotedAttributeValue

#name
switches to BeforeAttributeName when parsing whitespace
#data
<div a=val\ue>
#tokens
StartTag(div, a="val\ue", SelfClosing=false)
#errors
1:11 UnexpectedCharacterInUnquotedAttributeValue

#name
switches to CharacterReference when parsing Ampersand
#data
<div a=value 
#expected-state
BeforeAttributeName
#tokens

#name
Emits EOF instead of tag if EOF reached
#data
<div a=value&
#expected-state
CharacterReference
#tokens

#name
Batches characters and preserves content up to EOF
#data
<div a=value
#append-eof
#tokens
EndOfFile
#errors
1:13 EOFInTag

#name
switches to Data and emits the current tag when parsing GreaterThanSign
#data
<div a=value>
#tokens
StartTag(div, a="value", SelfClosing=false)
